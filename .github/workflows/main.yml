on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
          fetch-depth: 0

      - name: 📂📄 Update files on FTP server
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:
          ftp-server: ftpes://ftp-easyapply.alwaysdata.net
          ftp-username: easyapply_ftp
          ftp-password: ${{ secrets.SSH_PASSWORD }}
          local-dir: ./client # The directory to upload (relative to the root of the repository)

